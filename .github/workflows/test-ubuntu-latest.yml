name: Test ubuntu-latest

on:
  pull_request:
    types:
      - opened
      - synchronize
  push:
    branches:
      - main
      - dev

jobs:
  build:
    name: "Test ubuntu-latest"
    timeout-minutes: 30
    runs-on: ubuntu-latest

    steps:
      - name: "Update apt"
        run: sudo apt update
